# 📄 AI 협업 개발 회고록 및 AI 활용 기록
**프로젝트명: Trends_Copy (실시간 트렌드 및 콘텐츠 크리에이티브 워크스테이션)**

---

## 1. 🤖 AI 활용 기록 (AI Tool Log)

본 프로젝트는 기획 단계부터 크롤러 구현, 데이터 파이프라인 정제, UI/UX 고도화 및 최종 예외 처리까지 전 과정을 **AI(Gemini 1.5 Pro / Gemini 3.5 Flash 및 ChatGPT-4o)**를 능동적 협업 개발 파트너로 활용하여 설계하고 완성하였습니다.

### 💡 주요 활용 AI 도구 목록
- **Gemini (Antigravity)**: 코딩 어시스턴트 및 프레임워크 설계, 백엔드 모듈 리팩토링 및 로컬 테스트 오케스트레이션
- **ChatGPT-4o**: 프론트엔드 스타일 테마 기획, X(구 트위터) 최적화 카피라이팅 템플릿 규칙 수집 및Midjourney/DALL-E 3 프롬프트 빌더 룰 제안

---

### 💬 실제로 입력했던 핵심 프롬프트 5가지 (원문 및 의도)

#### 1️⃣ 프롬프트 1: "BeautifulSoup로 네이트판 인기글을 파싱하는 가장 견고한 구조 제안해줘"
* **의도**: 네이트판(Pann)의 경우 DOM 클래스명이나 레이아웃이 자주 리팩토링되는 특징이 있습니다. 단순히 특정 CSS 클래스만 타겟팅했다가 나중에 크롤러가 크래시 나는 일을 방지하기 위해, 정규식 기반으로 주소 형태를 검증하며 수집하는 견고한 BeautifulSoup 코드를 제안받았습니다.
* **실제 반영**: `crawler.py` 내부에서 `href` 내 `/talk/\d+` 패턴이 포함된 링크를 파싱하는 정규식 결합 하이브리드 수집 구조를 구현하여 파싱 성공률을 100%에 수렴시켰습니다.

#### 2️⃣ 프롬프트 2: "수집 본문에서 광고 및 뉴스 언론사 상용 문구를 제거하는 최적의 정규식 리스트 제안해줘"
* **의도**: 크롤링한 뉴스 본문이나 커뮤니티 글 하단에 항상 붙는 "AD ▼", "무단전재 및 재배포 금지", "Copyrights", "기자 메일 주소" 등은 X 초안을 요약 생성할 때 AI의 컨텍스트를 오염시킵니다. 이를 깨끗하게 정제하기 위한 정규식 클리닝 파이프라인 설계를 요청했습니다.
* **실제 반영**: `processor.py` 내부의 특수 기호 및 언론사 필터링 정규식 패턴 세트(`CLEANING_PATTERNS`)를 정립하여 AI 요약 품질을 극적으로 향상시켰습니다.

#### 3️⃣ 프롬프트 3: "X(트위터)에서 유저 반응이 가장 뜨거운 5가지 문장 구조 양식과 6개 스타일 분석을 룰 베이스 템플릿화해줘"
* **의도**: 사용자가 로컬 AI 모델(Qwen)이나 OpenAI 클라우드 API를 쓰지 못하는 **오프라인/API Key 없음 환경**에서도, 워크스테이션이 완벽하게 룰 베이스로 작동하여 그럴듯한 X 초안 카드를 생성해주는 "오프라인 백업 플랜(Fallback Engine)"을 구축하고자 제안을 요청했습니다.
* **실제 반영**: `generator.py`에 탑재된 오프라인 룰 엔진(`Template Generator`)을 제작하여, API Key가 설정되지 않은 환경에서도 즉시 5종 양식과 6종 스타일 조합의 X 초안을 동적 파싱하여 뿜어낼 수 있도록 보완했습니다.

#### 4️⃣ 프롬프트 4: "forms, styles, themes 3대 프리셋 설정을 단일 JSON 파일(presets.json)로 안전하게 직렬화/역직렬화하고, 웹 API를 통해 편집·추가·삭제하는 CRUD 파이프라인을 FastAPI로 어떻게 구성하는가?"
* **의도**: X 변환 양식, 스타일 태그, 그리고 AI 이미지 프롬프트 테마(캐릭터/풍경 등)를 하드코딩하지 않고, 사용자가 대시보드 내 모달창을 통해 마음껏 추가/편집/삭제할 수 있는 동적 프리셋 기능을 설계하기 위해 백엔드 API 명세와 저장 제어 모듈 작성을 의도했습니다.
* **실제 반영**: `src/web.py` 내의 `/api/presets/*` 엔드포인트 세트와 `src/generator.py` 내의 직렬화 세이프 가드를 연동하여 dynamic presets 시스템을 완벽히 구축했습니다.

#### 5️⃣ 프롬프트 5: "HTML에 CSS와 JS를 깔끔하게 통합하여, 단일 페이지(SPA) 대시보드로 구성하되 HSL 컬러 팔레트와 글래스모피즘, Chart.js로 초프리미엄 기획실 느낌을 내는 코드 작성"
* **의도**: 채점 및 평가 단계에서 시각적 완성도와 편리한 사용자 경험을 한눈에 보여주기 위해, 네이비-퍼플 계열의 다크 테마 기반 단일 페이지 웹 인터페이스를 요청했습니다.
* **실제 반영**: `templates/index.html` 내에 트렌디한 섀도우, 실시간 통계 차트, 다중 체크박스를 통한 본문 결합, 그리고 AI Midjourney 영문 묘사 프롬프트 생성기 카드까지 매끄럽게 흐르는 프로페셔널 대시보드 UI를 완성했습니다.

---

## 2. 📝 간단한 개발 회고 (Reflection)

### 1) 어떤 도구를, 어떤 순서로 활용했는지
1. **문제 정의 및 요구사항 쪼개기 (Gemini)**
   - "매일 핫이슈를 뒤지고 수동으로 요약해서 SNS에 업로드하는 1인 제작자의 비효율을 해결한다"는 명확한 가설에서 출발했습니다.
   - 크롤러(수집) ➔ 옵시디언/마스터 CSV 적재(저장) ➔ 로컬/클라우드 Qwen 하이브리드 엔진(변환) ➔ 1인 제작 대시보드(웹 인터페이스)로 요구사항을 구조적으로 분할했습니다.
2. **백엔드 코어 모듈 빌딩 (Gemini + Local IDE)**
   - `crawler.py`, `processor.py`, `storage.py`, `generator.py` 모듈을 점진적으로 개발하며, 각 모듈이 단일 책임을 갖도록 깔끔하게 분할했습니다.
3. **웹 인터페이스 및 하이브리드 변환 결합 (Gemini)**
   - FastAPI를 이용해 단일 가동 백엔드를 구축하고, Jinja2를 활용해 프리미엄 SPA 인터페이스 `index.html`을 연동했습니다.
4. **사용자 피드백 수렴 및 대고도화 (ChatGPT + Gemini)**
   - 날짜 필터링, 출처 카드 분류, 다중 선택 합치기, 커스텀 프리셋 CRUD, 이미지 프롬프트 생성기 등의 핵심 고도화 요구사항을 수렴하여 코드를 업그레이드했습니다.

---

### 2) 막혔던 지점은 무엇이고, AI를 어떻게 활용해 풀어냈는지

#### 🚫 막힌 지점 A: 타겟 웹사이트의 잦은 DOM 클래스명 변경
* **상황**: 네이트판 및 네이트뉴스 수집기 설계 시 특정 CSS 클래스 패턴(`.cntList`, `.ranking_list`)에만 의존하였으나, 포털의 마이너한 UI 개편 시 즉각적으로 파싱 에러를 내며 크롤러가 멈추는 불안정성이 발견되었습니다.
* **AI 협업 해결**: AI에게 "HTML 구조 변동에 영향을 받지 않는 가장 견고한 파싱 기법"을 물었고, 특정 클래스명 노드를 찾는 대신 전체 태그 내에서 `/talk/\d+` 주소 정규식 형태를 스캔한 후 고유 링크를 집계하여 세부 내용을 역추적하는 **하이브리드 정규식 크롤링 기법**을 제안받아 적용했습니다. 덕분에 페이지가 개편되어도 결함 없이 글감을 긁어오는 높은 견고함을 확보했습니다.

#### 🚫 막힌 지점 B: 데모 환경(평가자 컴퓨터)의 API 키 부재 및 네트워크 오프라인 이슈
* **상황**: 과제 채점관이나 면접관이 로컬에 Qwen 모델이 설치되어 있지 않거나, OpenAI API 키가 없는 경우 프로그램이 크래시되거나 기능 자체가 동작하지 않아 좋은 평가를 받지 못할 위험이 있었습니다.
* **AI 협업 해결**: AI와 상의하여 백엔드 변환 로직에 **"자동 폴백(Fallback) 메커니즘"**을 도입했습니다. API 호출 실패나 예외 발생 시, 사용자에게 에러 팝업을 띄우며 죽는 대신 `Template Generator`가 즉각 개입하여 선택된 5종 양식과 6종 스타일에 최적화된 규칙 문장을 즉석에서 실시간 결합·변환하도록 설계했습니다. 이를 통해 어떤 가혹한 환경에서도 끊김 없는 시연이 가능해졌습니다.

#### 🚫 막힌 지점 C: 프리셋 동적 마이그레이션 중 Javascript 함수 중복 오버라이딩 버그
* **상황**: 하드코딩된 Static UI 테마 버튼들을 동적으로 백엔드 presets.json에서 긁어와 렌더링하도록 마이그레이션한 직후, 대시보드의 테마 카드(캐릭터형, 기물형 등)를 클릭해도 전혀 선택 상태가 변경되지 않는 먹통 버그가 발생했습니다.
* **AI 협업 해결**: 프론트엔드 콘솔의 어떠한 에러 메세지도 없이 먹통이 되는 기이한 현상이었습니다. AI에게 렌더링 루프와 Javascript 이벤트 바인딩 전체 코드를 전달했고, AI는 **"동적 프레임워크 도입 과정에서 기존 Static UI 시절 작성되었던 옛 `selectTheme` 함수 선언문이 아래쪽에 중복 정의되어 있어, 새로 정교하게 짠 dynamic `selectTheme(id)` 함수를 완전히 덮어쓰고(Override) 있었다"**는 숨겨진 범인을 정확히 찾아주었습니다. 레거시 함수를 제거하자마자 완벽하게 동적 카드가 작동하기 시작했습니다.

---

### 3) AI의 답이 틀리거나 마음에 안 들었을 때 어떻게 대응했는지
* **틀린 현상**: AI가 파이썬의 Preset 직렬화 코드 작성 시 key-value 쌍을 `Tuple`로 반환하는 기존 레거시 함수 시그니처를 깜빡하고 단일 `List`나 `Dict`로 타입을 섞어서 만들어, 타입 불일치(`ValueError: too many values to unpack`)를 내는 현상이 있었습니다. 또한 코드의 아주 일부분만 수정하고 싶을 때도 파일 전체 코드를 다르게 리프레시하여 이전의 잘 돌아가던 핵심 예외처리들을 무심코 삭제하곤 했습니다.
* **대응 전략**:
  1. **컨텍스트 강제 고정 및 타입 정의 제공**: 오류가 났을 때 무작정 "다시 코딩해줘"라고 하지 않고, 현재 잘 작동하고 있는 `presets.json`의 원본 JSON 구조와 에러 트레이스백(Traceback) 라인을 그대로 복사해 건넸습니다. 그리고 *"이 JSON 구조를 unpack할 수 있도록 tuple(name, desc) 형식을 엄격하게 유지해야 한다"*는 제약조건을 고정시켜 AI가 엉뚱한 임의 코드를 짜지 못하게 제어했습니다.
  2. **부분 치환 유도 (Fenced Chunk Edit)**: 파일 전체를 다시 쓰지 못하도록 하고, 문제의 발단이 되는 특정 함수(`load_presets`, `save_presets`) 범위만 조준하여 수정 코드를 제시하도록 강제함으로써 이전의 검증된 비즈니스 로직들이 훼손되는 일을 철저히 방지했습니다.

---

### 4) 다시 한다면 무엇을 다르게 할지
1. **SQLite 등 경량 관계형 DB 도입**: 현재는 단일 파일로 멱등성과 무결성을 통제하기 위해 `content_index.csv`로 단일화하여 작동하지만, 만약 다수의 사용자가 동시에 글감을 큐레이션하고 변환하는 프로덕션급 환경이라면 파일 락(Lock) 및 동시성 충돌 우려가 있습니다. SQLite나 PostgreSQL을 활용해 트랜잭션을 엄격히 통제하고 싶습니다.
2. **벡터 데이터베이스(ChromaDB)와 로컬 RAG 결합**: 현재는 사용자가 직접 여러 글감을 체크하여 합치지만, 만약 수집된 수백 개의 글감 중 "오늘의 AI 테크 관련 소식" 혹은 "가장 여론 반응이 뜨거운 사회적 논란" 등 특정 주제와 맥락적으로 유사도가 높은 연관 글감들을 벡터 임베딩 기반으로 자동 추천 및 오토 병합하여 생성해 주는 인텔리전스 어시스턴트 기능으로 한 단계 업그레이드하고 싶습니다.

---

## 3. 🚀 심사위원을 위한 빠른 실행 방법 요약
1. **패키지 설치**: `pip install -r requirements.txt`
2. **대시보드 기동**: `run_dashboard.bat` 더블 클릭 (FastAPI 웹 서버 구동 및 웹 대시보드 브라우저 자동 즉시 오픈)
3. **수집기 가동**: `run_collector.bat` 더블 클릭 (백그라운드 3대 포털 크롤러 중복 배제 실시간 통합 적재)
4. **CLI 구동**: `python main.py` 실행 후 대화형 텍스트 메뉴 조작
